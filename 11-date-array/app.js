/**11-date-array*/

const arr = ['10-02-2022', '15-02-20222', 'тест', 'те.ст.те.ст', '1.02.2022', '10/02/2022', '10-02/2022', '-5-02-2022', '00-02-2022', '10-13-2022', '15/02/2022', '30-02-20222', '10/45/2022',];


const newArr = arr
    .map(el => el.split(''))
    .filter(el =>
        el.length === 10 && 
        (
            (el[2] === '.' && el[5] === '.') || 
            (el[2] === '-' && el[5] === '-') || 
            (el[2] === '/' && el[5] === '/')
        )
    )
    .map(el =>
        [
            Number(el.slice(0, 2).join('')),
            el.slice(2, 3).join(''),
            Number(el.slice(3, 5).join('')),
            el.slice(5, 6).join(''),
            Number(el.slice(6, 10).join(''))
        ]
    )
    .filter(el =>
        !el.includes(NaN) &&
        el[0] > 0 &&
        el[2] > 0 &&
        el[4] > 0
    )
    .filter(el =>
        (
            (el[1] === '.' || el[1] === '-') && 
            el[2] <= 12
        ) ||
        (   
            el[1] === '/' && el[0] <= 12
        )
    )
    .filter(el =>
        (
            (el[1] === '.' || el[1] === '-') &&
            (
                (
                    (el[2] === 1 || 3 || 5 || 7 || 8 || 10 || 12) &&
                    el[0] <= 31
                ) || 
                (
                    (el[2] === 2) &&
                    el[0] <= 28
                ) ||
                (
                    (el[2] === 4 || 6 || 9 || 11) &&
                    el[0] <= 30
                )
            )
        ) ||
        (
            (el[1] === '/') &&
            (
                (
                    (el[0] === 1 || 3 || 5 || 7 || 8 || 10 || 12) &&
                    el[2] <= 31
                ) || 
                (
                    (el[0] === 2) &&
                    el[2] <= 28
                ) ||
                (
                    (el[0] === 4 || 6 || 9 || 11) &&
                    el[2] <= 30
                )
            )
        )
    );
console.log(newArr);